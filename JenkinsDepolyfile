pipeline {
    agent any

    stages {
        stage('setup') {
            steps {
                echo 'setup'
            }
        }
        stage('build') {
            steps {
                echo 'build'
            }
        }
        stage('run unit test') {
            steps { a
                echo 'run unit test'
            }
        }
        stage('export ipa') {
            steps {
                echo 'export ipa'
            }
        }
        stage('upload to Fir') {
            steps {
                echo 'upload to Fir'
            }
        }
    }
}
